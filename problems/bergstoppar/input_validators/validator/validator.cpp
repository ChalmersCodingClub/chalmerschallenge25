#include <bits/stdc++.h>
#include "validator.h"


using namespace std;


void run() {
	int n = Int(2,Arg("maxn"));
	Endl();
	int h = Int(0,Arg("maxh"));
	Endl();
	for (int i = 0; i < n; i++)
	{
		int x = Int(Arg("minx"),Arg("maxx"));
		Space();
		int y = Int(0, Arg("maxh"));
		Endl();
	}

	Eof();
}
